# NOTE: For a truly pure development environment, use:
#   nix develop -i
#
# This .envrc loads the devShell but may not apply all overlays.
# The devShell is still clean and reproducible, just not as pure as -i

watch_file flake.nix
watch_file flake.lock
watch_file overlays/default.nix

use nix
