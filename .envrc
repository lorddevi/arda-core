# Automatically load Nix develop shell
# This provides a clean development environment with all project dependencies

watch_file flake.nix
watch_file flake.lock
watch_file overlays/default.nix

use flake
