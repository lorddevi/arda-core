# Arda Development Environment
# Automatically loads the Nix devShell when you enter this directory

watch_file flake.nix
watch_file flake.lock
watch_file overlays/default.nix

# Load the devShell from the flake
# This provides all project dependencies including Python 3.13 and rich-click 1.9.4
use nix
