# Automatically load Nix develop shell with pure environment
# The --ignore-environment flag ensures no system environment variables leak in

# Load nix-direnv stdlib
source_url "https://raw.githubusercontent.com/nix-community/nix-direnv/v3.0.0/fallback.nix"

# Use flake with ignore-environment flag
watch_file flake.nix
watch_file flake.lock
use flake --ignore-environment
